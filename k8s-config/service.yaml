apiVersion: v1
kind: Service
metadata:
  name: new-app-svc
  namespace: demo-app #change this
spec:
  type: NodePort
  selector:
    app: new-app          
  ports:
  - port: 80              
    targetPort: 3001      
    nodePort: 30092       
