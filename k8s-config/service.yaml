
apiVersion: v1
kind: Service
metadata:
  name: login-app
  namespace: startup
spec:
  selector:
    app: login-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: <APP_PORT>

  type: ClusterIP

